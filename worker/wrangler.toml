name = "travelviet-weather"
main = "weather.js"
compatibility_date = "2024-11-01"

# OPENWEATHER_API_KEY khong khai bao o day (secret, khong duoc commit).
# - Local dev: doc tu file .dev.vars trong thu muc nay (git-ignored).
# - Production: chay `npx wrangler deploy` tu trong thu muc worker/ (xem project.md muc 11).
#
# LUU Y QUAN TRONG: KHONG dat file wrangler.toml nay o goc repo. Cloudflare dang
# tu build/deploy Worker "travel-vibe-coding" (site tinh) tu goc repo moi khi push
# len GitHub — mot wrangler.toml o goc se bi build nham thanh Worker do, ghi de len
# site that (da xay ra va duoc rollback 2026-09-05, xem project.md muc 11.4).
